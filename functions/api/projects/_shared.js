export const SESSION_COOKIE_NAME='session_id';export function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{'Content-Type':'application/json'}})}export function error(message,status=400){return json({ok:false,error:message},status)}export function getCookie(request,name){const h=request.headers.get('Cookie')||'';for(const c of h.split(';').map(i=>i.trim()).filter(Boolean)){const s=c.indexOf('=');if(s===-1)continue;const n=c.slice(0,s),v=c.slice(s+1);if(n===name)return decodeURIComponent(v)}return''}export async function readBody(request){try{return await request.json()}catch{return{}}}export async function getUserFromSession(env,request){const sessionId=getCookie(request,SESSION_COOKIE_NAME);if(!sessionId)return null;const s=await env.DB.prepare(`SELECT sessions.user_id, users.email FROM sessions JOIN users ON users.id = sessions.user_id WHERE sessions.id = ? LIMIT 1`).bind(sessionId).first();return s?{id:s.user_id,email:s.email}:null}export function makeId(){const b=new Uint8Array(16);crypto.getRandomValues(b);return[...b].map(x=>x.toString(16).padStart(2,'0')).join('')}export function slugify(v){return String(v||'').toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'').slice(0,60)||'website'}